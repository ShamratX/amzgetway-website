# Worker package

This folder is the Cloudflare Worker.

**Setup docs (English):** see [`../docs/`](../docs/) and [`../README.md`](../README.md).

```bash
npm install
npx wrangler login
# edit wrangler.toml, create D1, migrate, set secrets
npm run deploy
```
